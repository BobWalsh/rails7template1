<nav class="bg-gray-800">
  <div class="px-6 mx-auto md:px-12">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <div class="inline pb-4 text-lg font-bold text-red-400">
            <%= heroicon "cloud"  %>
          </div>
          <a href="<%= root_path %>" class="text-2xl text-white">Weather App</h2>
        </div>
        <div class="hidden sm:block">
          <div class="flex items-baseline ml-8 space-x-4">
            <a href="<%= root_path %>" class="<%= nav_link_class("home") %>">Home</a>
            <a href="<%= articles_path %>" class="<%= nav_link_class("articles") %>">Articles</a>
            <a href="<%= users_path %>" class="<%= nav_link_class("users") %>">Users</a>
          </div>
        </div>
      </div>
      <div class="hidden sm:block">
        <div class="flex items-center ml-4 md:ml-6">
          <div class="relative ml-3">
            <% if user_signed_in? %>
              <a href="<%= edit_user_path %>" class="flex items-center max-w-xs text-sm bg-gray-800 rounded-full">
                <%= avatar(current_user) %>
              </a>
            <% else %>
              <%= link_to "Sign in", new_user_session_path, class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
            <% end %>
          </div>
        </div>
      </div>
      <div class="sm:hidden">
        <button class="<%= nav_link_class(nil) %>" onclick="toggleMenu()">
          <%= heroicon "bars-3" %>
        </button>
      </div>
    </div>
  </div>
  <div class="hidden sm:hidden mobile-menu">
    <div class="px-2 pb-3 space-y-2 sm:px-3">
      <a href="<%= root_path %>" class="<%= nav_link_class("home", "block") %>">Home</a>
      <a href="<%= articles_path %>" class="<%= nav_link_class("articles", "block") %>">Articles</a>
      <a href="<%= users_path %>" class="<%= nav_link_class("users", "block") %>">Users</a>
      <% unless user_signed_in? %>
        <a href="<%= new_user_session_path %>" class="<%= nav_link_class(nil, "block") %>">Sign in</a>
      <% end %>
    </div>
    <% if user_signed_in? %>
      <div class="py-4 border-t border-gray-700">
        <a href="<%= edit_user_path %>">
          <div class="flex items-center px-5">
            <div class="flex-shrink-0">
              <%= avatar(current_user) %>
            </div>
            <div class="ml-3">
              <div class="text-base font-medium leading-none text-white"><%= current_user.name %></div>
              <div class="text-sm font-medium leading-none text-gray-400"><%= current_user.email %></div>
            </div>
          </div>
        </a>
      </div>
    <% end %>
  </div>
</nav>
